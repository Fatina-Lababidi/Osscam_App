import 'package:flutter/material.dart';

class ListProjectsApp extends StatelessWidget {
  const ListProjectsApp({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}